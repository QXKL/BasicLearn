# 第 011 节: `@annotation`、`@within`、`@target`

## 这一节要学什么

- 基于注解的切点写法
- `@annotation` 的含义
- `@within` 的含义
- `@target` 的含义
- 自定义注解如何结合 AOP 使用

## 这一节的目标

学完后，你要能写出：

- 给某个方法注解后自动记录日志
- 给某类加注解后统一增强

## 后续会补的代码方向

- 自定义注解
- 注解驱动日志示例
- 注解驱动权限示例
